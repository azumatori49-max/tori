rules_version = '2';

// このアプリはサーバー(Admin SDK)経由でのみ Firestore にアクセスするため、
// クライアントからの直接アクセスはすべて拒否します。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
